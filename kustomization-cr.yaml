apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: my-infra
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: my-infra-repo
    namespace: flux-system
  path: "./clusters/flux-system"
  prune: true
  wait: true
  timeout: 10m
