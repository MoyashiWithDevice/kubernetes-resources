apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: proxmox-vault-secrets
  namespace: flux-system
spec:
  provider: vault
  parameters:
    vaultAddress: "http://vault.vault.svc.cluster.local:8200"
    roleName: "tf-ctrl-runner"
    objects: |
      - objectName: "endpoint"
        secretPath: "kv/data/proxmox"
        secretKey: "endpoint"
      - objectName: "api_token"
        secretPath: "kv/data/proxmox"
        secretKey: "api_token"
      - objectName: "username"
        secretPath: "kv/data/proxmox"
        secretKey: "username"
      - objectName: "password"
        secretPath: "kv/data/proxmox"
        secretKey: "password"

  # ★ここが重要：Kubernetes Secret に同期する
  secretObjects:
    - secretName: proxmox-auth
      type: Opaque
      data:
        - objectName: endpoint
          key: TF_VAR_endpoint
        - objectName: api_token
          key: TF_VAR_api_token
        - objectName: username
          key: TF_VAR_username
        - objectName: password
          key: TF_VAR_password
        
